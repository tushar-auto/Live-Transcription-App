import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tusharauto.livetranscription',
  appName: 'LiveTranscription',
  webDir: 'www'
};

export default config;
